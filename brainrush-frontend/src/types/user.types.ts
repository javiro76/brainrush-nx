/**
 * Tipos relacionados con el usuario
 */

export interface UserProfile {
  id: string;
  email: string;
  name: string;
  role: string;
  // Aquí puedes agregar otras propiedades específicas del perfil si son necesarias en el futuro
}
